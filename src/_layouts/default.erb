<!doctype html>
<html lang="<%= current_locale %>">
  <head>
    <%= render "head", metadata: site.metadata, title: data.title %>
  </head>
  <body class="<%= data.layout %> <%= data.page_class %> locale-<%= current_locale %>">
    <%= render Shared::Navbar.new(metadata: site.metadata, resource: resource, locale: current_locale) %>

    <main>
      <%= yield %>
    </main>

    <%= render "footer", metadata: site.metadata %>
  </body>
</html>
